{
  "name": "sass",
  "version": "1.0.0",
  "description": "sass loader by shoaib",
  "main": "index.js",
  "scripts": {
    "compile": "sass --watch --no-source-map scss:./../css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shoaib",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.26.7"
  }
}
